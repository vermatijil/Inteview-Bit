queue<int>pendingNodes;
    void Reverse(stack<int> &st)
    {
        if(st.size()==0) return;
        pendingNodes.push(st.top());
        st.pop();
        Reverse(st);
        st.push(pendingNodes.front());
        pendingNodes.pop();
    }
